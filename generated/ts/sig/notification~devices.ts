/** Matches `<notification>`. Server-initiated; nothing sends it. */
export function parseAccountSyncNotificationDevicesNotificationMixin(n: BinaryNode): AccountSyncNotificationDevicesNotificationMixin | null