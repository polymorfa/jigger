__d("WAWebHistorySyncUIText", ["fbt", "react"], (function(t, n, r, o, a, i, l, s) {
	var e, u = e || (e = o("react"));
	function c() {
		return s._(
			/*BTDS*/
			""
		);
	}
	c.displayName = c.name + " [from " + i.id + "]";
	var d = function(t) {
		return s._(
			/*BTDS*/
			"",
			[s._implicitParam("=m2", u.jsx("button", {
				className: "x1ph7ams",
				onClick: t,
				children: s._(
					/*BTDS*/
					""
				)
			}))]
		);
	};
	function m() {
		return s._(
			/*BTDS*/
			""
		);
	}
	m.displayName = m.name + " [from " + i.id + "]";
	function p() {
		return s._(
			/*BTDS*/
			""
		);
	}
	p.displayName = p.name + " [from " + i.id + "]";
	function _() {
		return s._(
			/*BTDS*/
			""
		);
	}
	_.displayName = _.name + " [from " + i.id + "]";
	function f(e) {
		return s._(
			/*BTDS*/
			"",
			[s._param("percentage", e, [0])]
		);
	}
	f.displayName = f.name + " [from " + i.id + "]";
	function g() {
		return s._(
			/*BTDS*/
			""
		);
	}
	g.displayName = g.name + " [from " + i.id + "]";
	function h(e) {
		return s._(
			/*BTDS*/
			"",
			[s._param("percentage", e, [0])]
		);
	}
	h.displayName = h.name + " [from " + i.id + "]";
	function y() {
		return s._(
			/*BTDS*/
			""
		);
	}
	y.displayName = y.name + " [from " + i.id + "]";
	function C() {
		return s._(
			/*BTDS*/
			""
		);
	}
	C.displayName = C.name + " [from " + i.id + "]";
	function b() {
		return s._(
			/*BTDS*/
			""
		);
	}
	b.displayName = b.name + " [from " + i.id + "]";
	function v() {
		return s._(
			/*BTDS*/
			""
		);
	}
	v.displayName = v.name + " [from " + i.id + "]";
	function S() {
		return s._(
			/*BTDS*/
			""
		);
	}
	S.displayName = S.name + " [from " + i.id + "]";
	function R() {
		return s._(
			/*BTDS*/
			""
		);
	}
	R.displayName = R.name + " [from " + i.id + "]", l.SYNC_LOADING = c, l.SYNC_IN_PROGRESS_CONTEXTUAL_DRAWER = d, l.SYNC_IN_PROGRESS_CONTEXTUAL_SYSTEM_MESSAGE_TEXT = m, l.SYNCING_OLDER_MESSAGES_MODAL_TITLE = p, l.SYNCING_OLDER_MESSAGES_MODAL_TEXT = _, l.SYNCING_OLDER_MESSAGES_MODAL_PERCENT = f, l.SYNCING_OLDER_MESSAGES_SETTINGS_TITLE = g, l.SYNCING_OLDER_MESSAGES_SETTINGS_PERCENT_COMPLETE = h, l.SYNCING_PAUSED_CONTEXTUAL_SYSTEM_MESSAGE_TEXT = y, l.SYNCING_PAUSED_CONTEXTUAL_TEXT = C, l.SYNCING_PAUSED_GLOBAL_SETTINGS_TITLE = b, l.SYNCING_PAUSED_GLOBAL_SETTINGS_SUBTITLE = v, l.SYNCING_PAUSED_POPUP_TITLE = S, l.SYNCING_PAUSED_POPUP_DESC = R;
}), 226);
