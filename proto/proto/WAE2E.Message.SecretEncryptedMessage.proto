message SecretEncryptedMessage {
  optional bytes targetMessageKey = 1;
  optional bytes encPayload = 2;
  optional bytes encIv = 3;
  optional bytes secretEncType = 4;
  optional bytes remoteKeyId = 5;
}