/** Matches `<iq>`. Server-initiated; nothing sends it. */
export function parseBizAccessTokenRequestSilentNonceResponseRecoveryRequired(n: BinaryNode): BizAccessTokenRequestSilentNonceResponseRecoveryRequired | null