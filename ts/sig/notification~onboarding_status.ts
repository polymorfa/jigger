/** Matches `<notification>`. Server-initiated; nothing sends it. */
export function parseCoexistenceOnboardingStatusNotificationRequest(n: BinaryNode): CoexistenceOnboardingStatusNotificationRequest | null