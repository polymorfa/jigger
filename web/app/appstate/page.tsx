import { EmptyDetail } from "@/components/ui";

export default function AppStateIndexPage() {
  return (
    <EmptyDetail>
      App-state mutations: how a star, mute, pin or label edit syncs between devices. The index
      name, collection and version have to all be right — get one wrong and two devices disagree
      forever, with no error on either side.
    </EmptyDetail>
  );
}
