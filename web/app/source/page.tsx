import { EmptyDetail } from "@/components/ui";

export default function SourceIndexPage() {
  return (
    <EmptyDetail>
      Pick a module to read it as shipped. These are the modules at least one fact was extracted
      from — a claim citing a module you cannot open is an assertion, not evidence.
    </EmptyDetail>
  );
}
