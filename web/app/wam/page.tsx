import { EmptyDetail } from "@/components/ui";

export default function WamIndexPage() {
  return (
    <EmptyDetail>
      Select an event to see its numbered field table. Events are sorted by wire id and their coverage
      across the three clients is shown as marks in the list.
    </EmptyDetail>
  );
}
